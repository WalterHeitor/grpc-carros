micronaut:
  application:
    name: grpcCarros
datasources:
  default:
    url: jdbc:mysql://localhost:3306/carros?useTimezone=true&serverTimezone=UTC&createDatabaseIfNotExist=TRUE
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: mariaclara
    schema-generate: CREATE_DROP
    dialect: MYSQL
jpa.default.properties.hibernate.hbm2ddl.auto: update
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true
        format_sql: true
